[test]
# Mutex in fixtures.ts serializes DB operations so parallel test files
# can safely share one SQLite database. Do NOT use --parallel=1 here —
# it causes worker isolation that loses tests (drops 563 → 539).
timeout = 60000
